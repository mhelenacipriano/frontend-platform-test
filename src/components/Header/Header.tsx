import React from "react";
import styles from "../../styles/Header.module.css";

export const Header = () => {
  return (
    <div className={styles.header}>
      <p className={styles.title}>Music.Ai</p>
    </div>
  );
};
